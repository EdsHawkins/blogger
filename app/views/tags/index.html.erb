<h1>All Tags</h1>

<ul id="tags">
  <% @tag.each do |tag| %>
    <li>
    	<%= link_to tag, tag_path(tag) %>
    </li>
  <% end %>
</ul>
<br>
<%= link_to "<< Back to articles list", articles_path %>

